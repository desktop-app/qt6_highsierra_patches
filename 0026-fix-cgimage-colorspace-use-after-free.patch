diff --git a/src/gui/image/qimage_darwin.mm b/src/gui/image/qimage_darwin.mm
--- a/src/gui/image/qimage_darwin.mm
+++ b/src/gui/image/qimage_darwin.mm
@@ -43,6 +43,8 @@
     if (!cgImageFormat)
         return nil;
 
+    QCFType<CGColorSpaceRef> colorSpace = cgImageFormat->colorSpace;
+
     // Create a data provider that owns a copy of the QImage and references the image data.
     auto deleter = [](void *image, const void *, size_t)
                    { delete static_cast<QImage *>(image); };
diff --git a/src/gui/painting/qcoregraphics.mm b/src/gui/painting/qcoregraphics.mm
--- a/src/gui/painting/qcoregraphics.mm
+++ b/src/gui/painting/qcoregraphics.mm
@@ -132,7 +132,11 @@
         format.redSize(), format.greenSize(), format.blueSize()
     });
 
-    QCFType<CGColorSpaceRef> colorSpace = [&]{
+    // The struct does not retain the color space, so it is returned
+    // with a +1 reference count, and the caller is responsible for
+    // releasing it once the CGImage or CGBitmapContext created from
+    // the format has taken its own reference.
+    CGColorSpaceRef colorSpace = [&]{
         if (const auto colorSpace = image.colorSpace(); colorSpace.isValid()) {
             QCFType<CFDataRef> iccData = colorSpace.iccProfile().toCFData();
             return CGColorSpaceCreateWithICCData(iccData);
@@ -140,6 +144,8 @@
             return CGColorSpaceCreateWithName(kCGColorSpaceSRGB);
         }
     }();
+    if (!colorSpace)
+        colorSpace = CGColorSpaceCreateWithName(kCGColorSpaceSRGB);
 
     return vImage_CGImageFormat{
         bitsPerComponent, format.bitsPerPixel(),
@@ -763,6 +769,8 @@
         return;
     }
 
+    QCFType<CGColorSpaceRef> colorSpace = cgImageFormat->colorSpace;
+
     context = CGBitmapContextCreate((void *)image->bits(), image->width(), image->height(),
         cgImageFormat->bitsPerComponent, image->bytesPerLine(), cgImageFormat->colorSpace,
         cgImageFormat->bitmapInfo);
diff --git a/src/gui/text/coretext/qfontengine_coretext.mm b/src/gui/text/coretext/qfontengine_coretext.mm
--- a/src/gui/text/coretext/qfontengine_coretext.mm
+++ b/src/gui/text/coretext/qfontengine_coretext.mm
@@ -781,6 +781,8 @@
     auto cgImageFormat = qt_mac_cgImageFormatForImage(im);
     if (!cgImageFormat)
         return im;
+
+    QCFType<CGColorSpaceRef> colorSpace = cgImageFormat->colorSpace;
 
     QCFType<CGContextRef> ctx = CGBitmapContextCreate(im.bits(), im.width(), im.height(),
         cgImageFormat->bitsPerComponent, im.bytesPerLine(), cgImageFormat->colorSpace,
